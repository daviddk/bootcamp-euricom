'use strict'

define([''], function() {
    return {
        add: function(x, y) {
            return x + y;
        },
        mul: function(x, y) {
            return x * y;
        }
    }
});

/*define([''], function() {

    function action() {
        console.log('hello');
    }

    return {
        action: action
    }
});
*/

/*
common JS

var obj = {
    name: 'test'
}
module.exports = obj;
*/
